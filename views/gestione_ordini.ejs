<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <title>Gestione Ordini</title>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Open+Sans&display=swap">
    <link rel="stylesheet" href="/css/style.css">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-icons/1.8.1/font/bootstrap-icons.min.css">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</head>
<body>
    <%- include('navbar', { user: user }) %>

    <div class="container mt-5">
        <h1>Gestione Ordini</h1>
        <p>In questa pagina puoi visualizzare e gestire gli ordini effettuati.</p>

        <table class="table table-striped">
            <thead>
                <tr>
                    <th>ID Ordine</th>
                    <th>Email Utente</th>
                    <th>Indirizzo</th>
                    <th>Numero di Telefono</th>
                    <th>Data</th>
                    <th>Totale</th>
                    <th>Azioni</th>
                </tr>
            </thead>
            <tbody>
                <% orders.forEach(order => { %>
                    <tr>
                        <td><%= order.ID_Ordine %></td>
                        <td><%= order.EmailUtente %></td>
                        <td><%= order.Indirizzo %></td>
                        <td><%= order.NumeroDiTelefono %></td>
                        <td><%= new Date(order.Data).toLocaleString() %></td>
                        <td>€<%= order.Totale.toFixed(2) %></td>
                        <td>
                            <a href="/dettagli_ordine/<%= order.ID_Ordine %>" class="btn btn-info btn-sm">Dettagli</a>
                        </td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
    </div>
    <%- include('footer') %>
</body>
</html>